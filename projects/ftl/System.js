"use strict"

class System {
	constructor(energy_capacity = 1, energy_given = 0, can_be_manned = false) {
		this.dom_container = docdiv(null);
		docspan(this.dom_container, name);
	}
}

class OxygenSystem extends System {
	constructor() {
		
	}
}